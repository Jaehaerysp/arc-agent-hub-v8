{
  "name": "arc-agent-hub-swap-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Minimal Express proxy that unblocks Circle Stablecoin Kit CORS for Arc Agent Hub's Swap feature. Proxies the Circle API HTTP call only — wallet signing always stays client-side in the connected browser wallet.",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
