// Runs once before the test suite. Extends Vitest's `expect` with
// jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.) for any
// future component-level tests.
import '@testing-library/jest-dom'
